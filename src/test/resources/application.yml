server:
  port: 8888

spring:
  profiles:
    active: local
  webflux:
    base-path: /assignment-product-service

logging:
  level:
    org.springframework: DEBUG
    liquibase: DEBUG
    LiquibaseSchemaResolver: DEBUG